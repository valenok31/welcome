const initialState = {
    listAttractions: [],
};


const siteManagement_reducer = (state = initialState, action) => {
    switch (action.type) {


        default:
            return state;
    }
};


export default siteManagement_reducer;